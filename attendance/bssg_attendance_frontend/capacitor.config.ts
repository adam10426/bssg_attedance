import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bssg_attendance_frontend',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
